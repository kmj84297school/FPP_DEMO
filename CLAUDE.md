# FPP (Football Player Potential AI) — Claude Code 인계 문서

> 이 문서는 claude.ai 웹 세션에서 진행한 FPP 2학년 재구축 작업의 공식 인계본이다.
> 여기 적힌 수치·상태는 2026-07-09 기준 실측값이며, 과장 없이 기록되었다.

## 0. 프로젝트 소유자와 절대 원칙

- 소유자: 고명준 (세마고 2학년). 학생부종합전형 진로활동의 메인 프로젝트이자 상업화 검토 대상.
- **기록 정확성 원칙 (모든 산출물에 적용):**
  - 실제로 수행하지 않은 것을 완성된 것처럼 표현 금지
  - 성능 수치는 검증 방법과 함께 기록 (in-sample vs out-of-fold 구분 명시)
  - 한계는 숨기지 않고 문서화 — 이 프로젝트의 정체성은 "정직한 측정"
- 보고서·발표 문구 작성 시: "완성된 기술"과 "확장 계획"을 반드시 분리

## 1. 프로젝트 한 문단 요약

FBref 유래 공개 데이터(worldfootballR 아카이브, 빅5 리그 2018~2023)를 기반으로,
축구 선수의 현재 능력을 이중 가중(포지션 렌즈 + 스타일 렌즈)으로 점수화하고,
"2~3년 후 실제 성과"를 라벨로 XGBoost 2단 모델(빅5 잔존 분류 + 미래 능력 회귀)을
학습시켰으며, 선수 단위 클러스터 부트스트랩으로 예측 신뢰구간을 산출·검증했다.
1학년 프로토타입(실시간 크롤링 + 규칙 기반)의 한계를 해소한 재구축 버전이다.

## 2. 아키텍처 대전환 (1학년 → 2학년)

```
[1학년] 요청 시 FBref 실시간 크롤링(Selenium) → 규칙 기반 점수 → 리포트
        문제: Cloudflare 차단, 느림, 서버 배포 불가, ML 불가능
[2학년] 사전 구축 데이터셋 → 사전 학습 모델(.json) → 서빙은 로드만
        결과: 크롤링 제로, 즉시 응답, 배포 가능, 실제 ML 학습 완료
```

## 3. 완료된 파이프라인 (파일 인벤토리)

zip들을 프로젝트 폴더에 풀면 아래 구조를 권장:

```
FPP_V2/
├── CLAUDE.md                          # 이 문서
├── data/
│   ├── fpp_features_clean_2018_2023.csv   # 필드플레이어 14,356행 × 169열
│   ├── fpp_features_GK_2018_2023.csv      # GK 1,166행 (별도 모델 대상, 미착수)
│   ├── coverage_by_year.csv               # 피처×연도 가용률 감사표
│   ├── fpp_ability_v1_2018_2023.csv       # 능력점수 산출본
│   ├── fpp_labeled_base.csv               # 라벨 부착 코호트 (초기 산출물, 참고용)
│   └── fpp_train_matrix_{u23,veteran}.csv  # 학습 행렬 — 06_build_cohorts.py가 생성
├── models/                                # 07_train_models.py가 생성 (코호트별 4종)
│   ├── xgb_survival_{u23,veteran}.json    # 잔존 분류기
│   ├── xgb_delta_{u23,veteran}.json       # Δ(변화량) 회귀기 — 레벨이 아님
│   └── resid_sigma_{u23,veteran}.npy      # 폴드 밖 잔차 sigma (구간 계산용)
├── src/
│   ├── scoring_v1.py                      # 이중 가중 능력점수 모듈
│   ├── boot_ci.py                         # 클러스터 부트스트랩 + 커버리지 검증
│   └── weights_v1.json                    # 포지션/스타일 가중치표
└── legacy/                                # 1학년 FPP_DEMO_LOCAL (참고용)
```

## 4. 핵심 설계 결정 (변경 시 소유자 승인 필요)

1. **능력점수 = (포지션 렌즈 + 스타일 렌즈) / 2** — 포지션 라벨의 거칢을 스타일
   분류가 완충 (예: 공격형 풀백 TAA가 포지션 49.5/스타일 69.1로 구제됨)
2. **분위수는 시즌×포지션 풀(600분+) 내에서 직접 계산** — FBref 제공 퍼센타일 미사용
3. **스타일 판별은 구성비(shape) 피처로, 점수 가중과 지표 분리** — 순환성 방지
4. **라벨 = t+2~t+3 자격 시즌(900분+)의 능력점수 평균** — 단일 시즌 노이즈 축소
5. **이탈(빅5 소멸)은 별도 분류 모델** — 회귀에 섞지 않음 (생존편향 명시 처리)
6. **교차검증은 GroupKFold(선수 단위)** — 동일 선수 훈련/검증 동시 포함 = 누수
7. **부트스트랩은 선수 단위 클러스터 리샘플링** — 행 단위는 군집 구조 위반
8. **신뢰구간 = sqrt(sigma_bootstrap² + sigma_residual²) 결합** — 순수 부트스트랩은
   커버리지 29.3%로 실패 실측됨. 결합 보정 후 82.0%/52.1% (명목 80/50과 일치)

## 5. 검증된 수치 (인용 시 이대로)

**현행 (2026-09, 3단계: A4 잔존 피처 + A3 튜닝 + 앙상블 평균 점추정).** 전부 GroupKFold(선수 단위) 5겹.
빌드 시 `build/_cache/model_metrics.json`에 실측값이 기록되고 사이트가 그걸 읽는다.

| 항목 | 성장기(U23) | 성숙기(24~38세) |
|---|---|---|
| 잔존 분류 AUC | 0.745 ± 0.020 | 0.759 ± 0.015 |
| 미래 능력 회귀 MAE (앙상블 평균) | 5.79 | 5.18 |
| R² | 0.346 | 0.521 |
| 80% / 50% 구간 실측 커버리지 | 81.1% / 51.7% | 82.9% / 53.3% |
| 75+ 밴드 (n, MAE, 편향, 80% 커버) | 19, 6.45, +1.46, 78.9% | 61, 4.93, +1.39, 85.2% |
| 코호트 행 수 (라벨 보유) | 2,268 (1,637) | 5,214 (2,800) |
| 잔존율 | 0.722 | 0.537 |
| 피처 수 | 64 (51 + A4 13) | 64 |

- 코호트: **2018~2022 시즌**(t+2·t+3이 둘 다 관측 가능한 해만), 900분+.
- 회귀 타깃은 **Δ = 미래능력 - 현재능력**, 점추정은 부트스트랩 앙상블(B=60) 평균.
- 하이퍼파라미터·피처셋: `build/hparams.json` (build/exp/a3a4_tune.py, 별도 분할 확인 후 채택).
- 3단계 실험 전체 기록: `dev_docs/PHASE3_REPORT.md` (기각된 실험 포함).

**직전 값 (2026-08, 코호트 확장 + Δ 타깃, 51피처 기본값):** U23 AUC 0.693 / MAE 6.00 / R² 0.306 /
커버리지 82.6%·52.2%, 베테랑 AUC 0.732 / MAE 5.31 / R² 0.500 / 83.2%·53.2%.

**이전 값 (2026-07 이전, 코호트 2018~2020 + 레벨 타깃).** 옛 문서·발표자료에
남아있을 수 있어 참고용으로만 보존: U23 AUC 0.712 / MAE 6.19 / R² 0.231,
베테랑 AUC 0.726 / MAE 5.33 / R² 0.502, 커버리지 82.0%/52.1%.
순수 부트스트랩만 썼을 때 커버리지가 29.3%로 실패했고, 잔차 sigma를 결합해
해결한 이력은 그대로 유효하다.

## 6. 남은 작업 (우선순위 순)

### A. 유사 선수 k-NN 레이어 — 완료
`build/lib/knn.py` + `04_knn_neighbors.py`(성장기) / `09_knn_veteran.py`(성숙기). 같은 포지션·나이±1,
DIST_COLS z-거리 k=10, 상위 10% 거리면 "비교 신뢰도 낮음". fbref_id로만 조인. 역할(A1) 기반 후보
축소는 실험 결과 중립~악화라 쓰지 않는다.

### B. 서빙 — FastAPI 대신 **정적 GitHub Pages**로 완료
크롤링·서버 없음. 빌드(`build/01…05`) → `docs/data/*.json` → 바닐라 JS + vendored Chart.js.
`main`의 `/docs`가 배포본. 페이지: `index.html`(검색·필터·permalink), `player.html`(상세·SHAP 설명),
`compare.html`(두 선수 비교). 구간은 `mu ± z·sqrt(σ_model² + σ_residual²)`, mu는 앙상블 평균.
디자인 시스템 "Orbit"(다크 유리 + 별·궤도 로고, FC26식 선수 카드, 모션): `dev_docs/DESIGN.md`.
비전문가용 활동 보고서: `dev_docs/ACTIVITY_REPORT.md` · 발표자료 프롬프트: `dev_docs/PPT_PROMPT.md`(+ `dev_docs/ppt_assets/`). 카드의 두 숫자는
"현재"/"2~3년 후 상한(80% 구간 상단)"으로만 부른다 — OVR/POT식 과장 금지.
(옛 계획의 Render/FastAPI 배포는 폐기. legacy app.py는 참고용으로만 남음.)

### B-2. 3단계(2026-09) 완료 요약 — 상세는 dev_docs/PHASE3_REPORT.md
- 채택: A4 잔존 피처 13개 + A3 튜닝(별도 분할 확인) + 앙상블 평균 점추정 → §5 지표.
  A5 SHAP 설명 패널·서술문. A8 인터페이스(필터·permalink·비교·모바일·접근성·190KB index).
  2023-24 반입 결함 2건 수정(Wolverhampton 2배 합산, 동명이인 6쌍 병합). A7 id 복구 1건(보수 규칙).
- 기각(지표 악화, 기록 보존): A1 역할 단위 채점(v2), A2 최댓값 라벨, A2 분위수 회귀.
- 데이터가 있어야 하는 것: dev_docs/DATA_REQUESTS.md (B1~B6). B2 검증 스크립트
  `build/exp/b2_external_validation.py`와 FBref↔TM 매핑(`data/fbref_tm_mapping_subset.csv`, 실 id 99.7%)은 준비됨.

### C. 최신 시즌 데이터 보충 — 2024·2025 반입 완료
현재 데이터: `fpp_features_clean_2018_2025.csv` **19,783행 × 169열** (2018~2025).
시즌별: 2018 2,365 / 2019 2,357 / 2020 2,411 / 2021 2,499 / 2022 2,575 /
2023 2,149 / 2024 2,928 / 2025 2,499.

- **2024-25 반입** (build/12_import_kaggle_2025.py, Kaggle hubertsidorowicz 단일 CSV):
  2,499명. fbref_id (정규화 이름, 생년) 매칭 → 1,518 기존 연결 / 981 신규(합성 n25xxxxx) / 모호 0.
- **2023-24 반입** (build/13_import_2024.py, 리그/팀 폴더별 카테고리 CSV 묶음):
  2,928명. 팀 단위로 8개 카테고리 파일을 Player 키 병합 후 전체 결합.
  Born이 팀별 파일에 없어 같은 시즌 표준 테이블(top5-players.csv)에서 이름으로 보충.
  id 매칭: 2,146 기존 연결 / 341 신규 / 127 이름유일성으로 복구 / 314 미해결(합성 n24xxxxx).
  Age에서 Born 역산은 생일이 8/1 이후면 ±1년 어긋나 오결합 위험 → 하지 않음.
  집계행('Squad Total'/'Opponent Total') 제거, Wolverhampton 폴더 중복 병합 처리.
- **효과**: 2025 스냅샷 2,499명 중 **1,351명(54%)이 직전 시즌 이력 확보** →
  Δ(성장궤적) 피처가 실제로 살아남 (예측대상 475명 중 264명). 반입 전에는 전량 결측이었다.
- **미반입**: 2025-26 파일은 고급지표(패스·포제션·수비·GCA 블록) 자체가 없어
  (102컬럼) 능력점수 계산 불가 → 의도적으로 제외.
- **결손 컬럼**: 2025는 pas_Att_Long 부재 → shape_long 판별축 1개 결측 (2024는 있음).
  2023 사망 컬럼 20개 목록은 coverage_by_year.csv 참고.

### C-2. 상위권 외삽 한계 — 2026-08 진단 후 **해결**
아래는 진단 기록이다. 해결책(Δ 타깃 + 코호트 확장)은 이 절 맨 끝에 정리했다.

"현재 88점인 선수의 2~3년 후 예측이 왜 73점인가"에 대한 조사 결과:

- **스케일 불일치 아님**: 라벨과 현재 점수는 동일 산식. 학습 데이터에서 U23 코호트는
  현재 평균 53.3 → 실제 관측된 미래 평균 53.8(+0.5)로 체계적 편향 없음.
- **보정 가능한 편향도 아님**: OOF 보정회귀 기울기 U23 0.870 / 베테랑 0.926 (1 미만).
  선형 보정을 적용해도 MAE가 개선되지 않음(5.98→5.98, 5.33→5.31). 모델이 실제로
  예측하는 구간(~74점 이하)에서는 구간별 편향이 최대 -2.2로 잘 맞는다.
- **진짜 원인은 라벨 범위**: 학습 라벨(fut_ability_v2)에 85점 이상이 **0명**이고
  99%tile이 U23 73.7 / 베테랑 76.4. 트리 모델은 학습 라벨 밖으로 외삽하지 못하므로
  mu의 구조적 상한이 73~74다. 현재 능력이 그보다 높은 선수는 무엇을 하든 하락 예측이
  나온다. 실증적으로 80~85 구간 선수의 실제 하락폭은 -6.8인데 모델은 -16을 예측.
- **왜 라벨만 낮은가**: ① 단일 시즌 85점+ 자체가 전체에서 4명뿐 ② 라벨은 t+2·t+3
  두 시즌 평균이라 상위 1%가 추가로 1.5점 압축 ③ 시즌 간 상관 0.698 — 극단 시즌의
  30%가량은 그 해 한정 변동.
- **1차 대응(진단 시점)**: 현재 능력이 해당 모델 라벨의 99%tile을 넘는 선수(당시
  22명, 1.5%)에게 "모델 검증 범위 밖" 경고를 노출하고 k-NN 실증 결과를 우선
  참고하도록 안내. 점추정을 임의로 올리지는 않았다.

**해결 (2026-08 적용): 회귀 타깃을 Δ로 전환 + 코호트 확장**

- **Δ 타깃**: 레벨(미래 능력)을 직접 맞추는 대신 **Δ = 미래능력 - 현재능력**을
  학습하고, 예측 시 그 선수의 실제 현재 점수에 더한다. Δ는 0 근처에 분포하므로
  라벨 상한의 저주가 사라진다. OOF 비교에서 두 코호트 모두 개선 —
  U23 MAE 5.98→5.89 / R² 0.296→0.315, 베테랑 5.33→5.28 / 0.507→0.512.
  예측 천장 71~76 → 78~80.
- **코호트 확장**: 2024·2025 반입 덕에 2021·2022 코호트를 추가할 수 있게 됨
  (t+2·t+3 모두 관측 가능한 2018~2022 사용). 라벨 U23 993→1,638, 베테랑
  1,774→2,799. 단, **이것만으로는 상한 문제가 안 풀린다** — 어느 코호트 연도로
  만들어도 85점 이상 라벨은 여전히 0명이다(2시즌 평균이라 구조적으로 불가능).
- **효과**: 하피냐 88.7 → mu 72.8이던 것이 **82.1**로 (실증 하락폭 -6.8과
  거의 일치). 올리세 74.8→80.4, 야말 69.5→80.3.
- **여전히 남는 것**: Δ 방식도 학습 범위 밖에서는 "변화 없음"으로 수렴하는
  낙관 편향이 있다. 그래서 검증범위 밖 경고(19명, 1.3%)는 유지한다.
- **미착수 대안**: 라벨을 2시즌 평균 대신 최댓값으로, 분위수 회귀로 상단 직접 추정.

### C-4. 예측 설명(SHAP) — 완료 (2026-09, dev_docs/PHASE3_REPORT.md A5)
build/03_predict.py가 XGBoost pred_contribs(TreeExplainer SHAP 동치)로 선수별 Δ회귀·잔존분류
기여도 상위 5개를 산출 → 선수 JSON `explanation` → 상세페이지 "왜 이 예측인가" 패널.
자동 서술문(narrative_potential)도 기여도 근거로 작성. 기여도는 국소 설명이지 인과가 아님을 화면에 명시.

### C-3. 포지션 세분화(역할 7분류) 실험 — 점수 개편 기각 (2026-09, dev_docs/PHASE3_REPORT.md A1)
shape z-축으로 CB/FB/DM/CM/AM/W/ST를 추론해 시즌×역할 풀로 채점하는 v2(src/scoring_v2.py)를
같은 코호트·같은 GroupKFold로 비교한 결과, 역할 판정은 건전(알려진 선수 13/16)하지만
점수 재현성이 떨어져(기저 MAE 6.65→8.03) OOF MAE가 U23 6.00→7.10, 베테랑 5.31→6.39로 악화.
역할 one-hot 피처·역할 기반 k-NN 후보 풀도 중립~미세 악화. **점수는 v1 유지**, 역할은
표시용 참고 라벨(`role`, `role_axis`)로만 산출. 포지션 간 분산 불일치는 미해결.

### D. 백로그 (착수 전 소유자와 상의)
- 능력점수 v2: 박스 포처 저평가(그룹 내 균등가중 한계), 수비수 팀 강도 편향,
  포지션 간 분산 불일치(FW sd 13.6 vs DF 7.5) — 진단 완료. 역할 단위 채점으로는 안 풀림(A1 기각).
  다음 후보: 시즌×포지션 내 표준화(50+10z) — 라벨 정의가 바뀌므로 §4 승인 필요.
- 2022-23(=2023) 시즌이 원 아카이브 자체가 13경기 부분 시즌(최대 1,170분)이라 코호트 2020 t+3 /
  2021 t+2 라벨이 900분 기준을 거의 못 넘긴다. 부분 시즌 비례 임계(≈308분) 적용 여부는 라벨 정의 변경.
- A3 선택 설정이 격자 경계(lr 0.03, 트리 150, 깊이 3)에 있다 — 더 강한 정규화 탐색 여지.
- 외부 라벨 검증(B2): 데이터만 오면 즉시 실행 가능(스크립트·매핑 준비됨).
- GK 전용 모델(B6), 빅5 밖 리그 라벨 분리(B4), 부상 기록(B3): DATA_REQUESTS.md 참조.

## 7. 데이터 출처·라이선스 (상업화 관련 중대사항)

- 데이터: worldfootballR 공개 아카이브 (github.com/JaseZiv/worldfootballR_data),
  원출처 FBref(Stats Perform/Opta 유래)
- 교육·연구 사용: 문제없음 (출처 명기)
- **상업 서비스에 이 데이터 직접 사용: 불가** — 라이선스 위반 리스크
- 상업화 경로는 BYOD(고객 데이터 반입형) 또는 정식 라이선스 확보 전제.
  Stats Perform 문의 이력 있음 (2025.10, 자동회신 수신)

## 8. 실행 환경 메모

- Python 3.12, 주요 의존성: pandas, numpy, xgboost, scikit-learn, fastapi,
  uvicorn, jinja2, matplotlib
- pip 설치 시 시스템에 따라 --break-system-packages 필요할 수 있음
- 모델 로드: `XGBRegressor(); m.load_model("models/xgb_delta_u23.json")` (서빙 mu는 03의 앙상블 평균)
- 전체 재빌드 순서: `01 → 06 → 07 → 02 → 03 → 04 → 09 → 10 → 11 → 05` (`build/exp/*`는 실험용, 파이프라인 아님)
- 스크린샷/스모크 테스트: Playwright + `/opt/pw-browsers/chromium`, `docs/`를 `python3 -m http.server`로 서빙
